apiVersion: v1
kind: Secret
metadata:
  name: manager-bootstrap-credentials
  namespace: system
type: Opaque
data:
  credentials.json: ${METALSOFT_B64ENCODED_CREDENTIALS}
# stringData:
#   credentials.env: |-
#     METALSOFT_USER_EMAIL=${METALSOFT_USER_EMAIL}
#     METALSOFT_API_KEY=${METALSOFT_API_KEY}
#     METALSOFT_ENDPOINT=${METALSOFT_ENDPOINT}
#     METALSOFT_LOGGING_ENABLED=${METALSOFT_LOGGING_ENABLED}
